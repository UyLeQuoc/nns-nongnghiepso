/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['techcrunch.com',"firebasestorage.googleapis.com", "developers.elementor.com"],
    }
};

export default nextConfig;
